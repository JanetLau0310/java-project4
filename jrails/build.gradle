plugins {
    id 'java'
}

version '1.0-SNAPSHOT'

sourceCompatibility = 1.13

dependencies {
    testCompile project(':books')
    testImplementation project(':books')
    testCompile deps.hamcrest
    testCompile deps.jUnit
}